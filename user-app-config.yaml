apiVersion: v1 
kind: ConfigMap
metadata:
  name: app-config
  namespace: my-namespace
data: # store environment variables for application deployments.
  NODE_ENV: "production"
  DB_HOST: "song-db"
  DB_PORT: "5432"